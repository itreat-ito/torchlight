{
  "name": "torchlight",
  "version": "1.0.0",
  "description": "Display a banner to visually distinguish between local, staging, and production environments",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "package": "npm run build && cd dist && zip -r ../torchlight-extension.zip . && cd .. && echo '配布用zipファイルを作成しました: torchlight-extension.zip'"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.21",
    "sass": "^1.77.0",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "@melloware/coloris": "^0.25.0",
    "micromodal": "^0.6.1",
    "sortablejs": "^1.15.6",
    "toastify-js": "^1.12.0"
  }
}
